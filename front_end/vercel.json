# Vercel configuration for React frontend (optional, for Vercel deploys)
{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "http://localhost:3000/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
